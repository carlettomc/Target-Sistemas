#include <stdio.h>

int main() {
  int indice = 13, soma = 0, k = 0;

  while (k < indice) {
    k = k + 1;
    soma = soma + k;
  }

  printf("A soma dos números inteiros de 1 até %d é %d.\n", indice, soma);

  return 0;
}
